export default {async fetch(req){const u=new URL(req.url);const lat=u.searchParams.get('lat'),lon=u.searchParams.get('lon');if(!lat||!lon)return new Response('Missing lat/lon',{status:400});const r=await fetch(`https://globalwindatlas.info/api/gwa/custom/Lib/?lat=${lat}&long=${lon}`,{headers:{Referer:'https://globalwindatlas.info'}});return new Response(await r.text(),{status:r.status,headers:{'content-type':'text/plain; charset=utf-8','access-control-allow-origin':'*'}})}};
