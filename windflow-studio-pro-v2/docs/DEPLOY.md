# Deploy
Upload the `windflow-studio-pro-v2` folder to GitHub Pages, or replace the existing `windflow-studio/` folder with these files.

For automatic GWA download, deploy `worker/cloudflare-gwa-proxy.js` as a Cloudflare Worker and paste the worker URL in the GWA Proxy URL field.
