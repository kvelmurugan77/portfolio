import {S} from './state.js';
export function drawMap(){const cv=document.getElementById('mapCanvas');if(!cv)return;const ctx=cv.getContext('2d'),W=cv.width,H=cv.height;ctx.clearRect(0,0,W,H);ctx.fillStyle='#0b1220';ctx.fillRect(0,0,W,H);let pts=[];if(S.terrain){for(let i=0;i<S.terrain.ny;i++)for(let j=0;j<S.terrain.nx;j++)pts.push({lat:S.terrain.lat0+i*(S.terrain.lat1-S.terrain.lat0)/(S.terrain.ny-1),lon:S.terrain.lon0+j*(S.terrain.lon1-S.terrain.lon0)/(S.terrain.nx-1),z:S.terrain.grid[i][j]})}pts.push(...S.turbines);if(!pts.length)return;const minLat=Math.min(...pts.map(p=>p.lat)),maxLat=Math.max(...pts.map(p=>p.lat)),minLon=Math.min(...pts.map(p=>p.lon)),maxLon=Math.max(...pts.map(p=>p.lon));const x=lon=>40+(lon-minLon)/(maxLon-minLon||1)*(W-80),y=lat=>H-40-(lat-minLat)/(maxLat-minLat||1)*(H-80);if(S.terrain){const T=S.terrain;for(let i=0;i<T.ny-1;i++)for(let j=0;j<T.nx-1;j++){const z=(T.grid[i][j]+T.grid[i+1][j]+T.grid[i][j+1]+T.grid[i+1][j+1])/4;const hue=220-(z-T.minE)/(T.maxE-T.minE||1)*180;ctx.fillStyle=`hsla(${hue},80%,50%,.45)`;const x1=x(T.lon0+j*(T.lon1-T.lon0)/(T.nx-1)),x2=x(T.lon0+(j+1)*(T.lon1-T.lon0)/(T.nx-1)),y1=y(T.lat0+i*(T.lat1-T.lat0)/(T.ny-1)),y2=y(T.lat0+(i+1)*(T.lat1-T.lat0)/(T.ny-1));ctx.fillRect(Math.min(x1,x2),Math.min(y1,y2),Math.abs(x2-x1)+1,Math.abs(y2-y1)+1)}}ctx.strokeStyle='#c9d1d9';ctx.globalAlpha=.5;for(const c of S.contours){ctx.beginPath();for(const seg of c.segs){ctx.moveTo(x(seg[0].lon),y(seg[0].lat));ctx.lineTo(x(seg[1].lon),y(seg[1].lat))}ctx.stroke()}ctx.globalAlpha=1;ctx.fillStyle='#58a6ff';ctx.strokeStyle='#fff';for(const t of S.turbines){ctx.beginPath();ctx.arc(x(t.lon),y(t.lat),5,0,Math.PI*2);ctx.fill();ctx.stroke()}ctx.fillStyle='#c9d1d9';ctx.font='13px monospace';ctx.fillText(`${S.turbines.length} turbines`,16,22)}
